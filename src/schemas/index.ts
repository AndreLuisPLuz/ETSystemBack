import { loginSchema } from "./login.schema"
import { postUserSchema, updateUserSchema } from "./user.schema"
import { postInstitutionSchema, updateInstitutionSchema } from "./institution.schema"

export {
    loginSchema,
    postUserSchema,
    updateUserSchema,
    postInstitutionSchema,
    updateInstitutionSchema,
}